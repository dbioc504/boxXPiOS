export const colors = {
    text: '#FFFF00',
    background: '#00008B'
}

export const fonts = {
    heading: 'FugazOne',
}

export const theme = {
    colors,
    fonts
}

export type Theme = typeof theme;